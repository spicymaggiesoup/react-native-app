import React from 'react';
import { StyleSheet, Button } from 'react-native';

export const submitButton = () => {
    const fnTest = () => {

    };
    return (<Button title="확인" onPress={fnTest}/>);
};

const styles = StyleSheet.create({

});